# Runtime. Pinned exactly: a hash chain written by one version of pydantic must
# be verifiable by the version the judges run, and canonical JSON is only
# reproducible if serialisation behaviour is.
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
PyYAML==6.0.3
cryptography==50.0.0
anthropic==0.120.2
python-dotenv==1.2.2

# Development. Install with: pip install -r requirements.txt -r requirements-dev.txt
